# Exercise: Flatland Space Stations
# URL: https://www.hackerrank.com/challenges/flatland-space-stations/problem?isFullScreen=true
# Description: Find the maximum distance between a station and a city

def flatlandSpaceStations(n, c):
    c.sort()
    max_distance = c[0]
    
    for i in range(1, len(c)):
        max_distance = max(max_distance, (c[i] - c[i-1]) // 2)
    
    max_distance = max(max_distance, n - 1 - c[-1])
    return max_distance
